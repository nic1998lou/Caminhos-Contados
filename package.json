{
      "name": "caminhos-contados",
      "version": "1.0.0",
      "description": "Um aplicativo de guia turístico de áudio interativo.",
      "main": "index.tsx",
      "scripts": {
        "build": "esbuild index.tsx --bundle --outfile=dist/main.js --format=esm --jsx=automatic --define:process.env.NODE_ENV='\"production\"' --external:react --external:react-dom/client --external:react/jsx-runtime",
        "start": "echo \"Abra o index.html no seu navegador para desenvolvimento local ou use um servidor de desenvolvimento simples.\""
      },
      "keywords": ["react", "typescript", "geolocation", "audio", "guide"],
      "author": "Seu Nome",
      "license": "ISC",
      "devDependencies": {
        "esbuild": "^0.20.0"
      }
    }